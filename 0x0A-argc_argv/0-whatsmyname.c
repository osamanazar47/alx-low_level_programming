#include <stdio.h>
/**
 * main - function for printing the name of the program
 * @argc: for number of arguments
 * @argv: an array of strings of the arguments
 * Return: o for success
 */
int main(int argc, char *argv[])
{
	(void)argc;
	puts(argv[0]);
	return (0);
}
