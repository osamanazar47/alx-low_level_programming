#include "main.h"
#include <stdio.h>
/**
 * _strlen - return the length of a string
 * @s: the string
 * Return: the length of the of the string
 */
int _strlen(char *s)
{
	size_t length = strlen(s);
}
