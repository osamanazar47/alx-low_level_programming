#include "main.h"

/**
 * @n: the integr that we will compute
 *
 * _abs - a function that computes the absolute value of an integer
 * 
 * Return: 0 for success
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}
