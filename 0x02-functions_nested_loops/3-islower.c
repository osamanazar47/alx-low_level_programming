#include "main.h"
/**
 * main - a function for checking lowercase letter c
 *
 * Return: 0 as always
 */

int _islower(int c)
{
	char testchar = 'c';

	if (_islower(testchar))
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
