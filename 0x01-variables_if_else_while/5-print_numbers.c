#include <stdio.h>
/**
 * main - function for printing the base 10 numbers
 *
 * Return: 0 is for success
 */
int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
	{
		printf("%d", i);
	}
	return (0);
}
